<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SetmealMapper">
    <resultMap id="SetmealResultMap" type="com.sky.entity.Setmeal">
        <id column="id" property="id"/>
        <result column="category_id" property="categoryId"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
        <result column="status" property="status"/>
        <result column="description" property="description"/>
        <result column="image" property="image"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
    </resultMap>
	
	<!-- 查询的代码分页 -->
    <select id="page" resultMap="SetmealResultMap">
        SELECT * FROM (
            SELECT A.*, ROWNUM RN FROM (
                SELECT * FROM setmeal
                <where>
                    <if test="name != null and name!=''">
                        name like concat('%',#{name},'%')
                    </if>
                    <if test="categoryId != null">
                        category_id = #{categoryId}
                    </if>
                    <if test="status != null">
                        status = #{status}
                    </if>
                </where>
                ORDER BY create_time DESC
            ) A WHERE ROWNUM &lt;= #{start}+#{pageSize}
        ) WHERE RN &gt; #{start}
    </select>
	
	<!-- 总页数 -->
    <select id="count" resultType="java.lang.Long">
        select count(*) from setmeal
        <where>
            <if test="name != null and name!=''">
                name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId}
            </if>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
    </select>
	
	<!-- 新增套餐 -->
    <insert id="insert">
        insert into setmeal(category_id,name,price,status,description,image,create_time,update_time,create_user,update_user)
        values(#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
	
	<!-- 修改套餐 -->
    <update id="update">
        update setmeal
        set category_id=#{categoryId},
            name=#{name},
            price=#{price},
            status=#{status},
            description=#{description},
            image=#{image},
            update_time=#{updateTime},
            update_user=#{updateUser}
        where id=#{id}
    </update>
	
	<!-- 根据id查询套餐 -->
    <select id="getById" resultMap="SetmealResultMap">
        select * from setmeal where id=#{id}
    </select>
	
	<!-- 删除套餐 -->
    <delete id="deleteById">
        delete from setmeal where id=#{id}
    </delete>
</mapper>
