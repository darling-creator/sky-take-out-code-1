<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.EmployeeMapper">

    <select id="getByUsername" resultType="Employee">
        select * from employee where username = #{username}
    </select>

    <select id="page" resultType="Employee">
        select * from employee
        <where>
            <if test="name != null and name != ''">
                name like concat('%',#{name},'%')
            </if>
        </where>
    </select>

    <insert id="insert">
        insert into employee(username,name,password,phone,status,create_time,update_time)
        values(#{username},#{name},#{password},#{phone},#{status},#{createTime},#{updateTime})
    </insert>

    <select id="getById" resultType="Employee">
        select * from employee where id = #{id}
    </select>

    <update id="update">
        update employee
        set username=#{username},name=#{name},phone=#{phone},status=#{status},update_time=#{updateTime}
        where id = #{id}
    </update>
</mapper>
