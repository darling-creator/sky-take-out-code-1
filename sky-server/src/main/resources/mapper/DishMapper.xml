<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.DishMapper">
    <resultMap id="DishResultMap" type="com.sky.entity.Dish">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
    </resultMap>

    <!--查询的分页查询-->
    <select id="page" resultMap="DishResultMap">
        SELECT * FROM (
            SELECT A.*, ROWNUM RN FROM (
                SELECT * FROM dish
                <where>
                    <if test="name != null and name!=''">
                        name like concat('%',#{name},'%')
                    </if>
                    <if test="categoryId != null">
                        category_id = #{categoryId}
                    </if>
                    <if test="status != null">
                        status = #{status}
                    </if>
                </where>
                ORDER BY create_time DESC
            ) A WHERE ROWNUM &lt;= #{start}+#{pageSize}
        ) WHERE RN &gt; #{start}
    </select>
	
	<!-- 查询的统计总数 -->
    <select id="count" resultType="java.lang.Long">
        select count(*) from dish
        <where>
            <if test="name != null and name!=''">
                name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId}
            </if>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
    </select>
	
	<!-- 新增菜品 -->
    <insert id="insert">
        insert into dish(name,category_id,price,image,description,status,create_time,update_time,create_user,update_user)
        values(#{name},#{categoryId},#{price},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
	
	<!-- 修改菜品 -->
    <update id="update">
        update dish
        set name=#{name},
            category_id=#{categoryId},
            price=#{price},
            image=#{image},
            description=#{description},
            status=#{status},
            update_time=#{updateTime},
            update_user=#{updateUser}
        where id=#{id}
    </update>
	
	<!-- 根据id查询菜品 -->
    <select id="getById" resultMap="DishResultMap">
        select * from dish where id = #{id}
    </select>
	
	<!-- 根据id删除菜品 -->
    <delete id="deleteById">
        delete from dish where id=#{id}
    </delete>
    
    <!-- 批量插入口味 -->
    <insert id="insertBatch">
        insert into dish_flavor(dish_id,name,value)
        <foreach collection="flavors" item="item" separator="union all">
            select #{item.dishId},#{item.name},#{item.value} from dual
        </foreach>
    </insert>
</mapper>
